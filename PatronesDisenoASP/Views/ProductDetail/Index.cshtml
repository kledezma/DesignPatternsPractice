@{
    decimal totalLocal = (decimal)ViewBag.totalLocal;
    decimal totalF = (decimal)ViewBag.totalForeign;
}

<div class="row">

    <div class="col-12">
        <label>Total Local</label>
        <b>@totalLocal</b>
    </div>
    <div class="col-12">
        <label>Total Local</label>
        <b>@totalF</b>
    </div>

</div>